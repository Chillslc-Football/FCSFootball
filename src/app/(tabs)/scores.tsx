import { Screen } from '@/components/Screen';

export default function ScoresScreen() {
  return (
    <Screen
      title="Scores"
      subtitle="Live and final scores from FCS games."
    />
  );
}
